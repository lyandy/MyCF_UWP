<UserControl
    x:Class="MyCF.UIControl.DataTemplates.CategoryUIControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyCF.UIControl.DataTemplates"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Height="48" Background="Transparent" Width="180" Tapped="Grid_Tapped">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Image Source="{Binding Path=IconBitmap}" Width="18" Height="18" Margin="3"/>
        <!--<TextBlock Text="{Binding Path=TextIcon}" Foreground="White" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center"/>-->
        <TextBlock Grid.Column="1" Text="{Binding Path=Title}" VerticalAlignment="Center" Margin="30,0,0,0" Foreground="White"/>
    </Grid>
</UserControl>
